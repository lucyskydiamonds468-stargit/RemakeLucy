name: edgex-lucy-bot
services:
  - name: bot
    type: worker
    regions: [tyo]           # Tokyo
    instance:
      type: cpu-micro        # CPU Standard Micro
    build:
      builder: buildpack
      run_command: "python main.py"  # ← これが必須！
